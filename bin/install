#!/usr/bin/env bash
set -eo pipefail

PLUGIN_DIR="$(dirname "${BASH_SOURCE[0]}")/.."
source "$PLUGIN_DIR/lib/helpers.sh"

unzip "${ASDF_DOWNLOAD_PATH}/clj-kondo.zip"
install ./clj-kondo "${ASDF_INSTALL_PATH}"
